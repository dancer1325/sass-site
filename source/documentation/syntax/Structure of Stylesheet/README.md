# Statements

# Expressions
* := Right hand side of
  * CSS declaration
  * variable declaration
* produces a value
  * compile and check how the expression is replaced by a value
* uses
  * arguments passed to 
    * `@mixin` and `@include`
    * `@function`
  * control flow
    * `@if` and `@else`
  * manipulation via numeric operators -- Check '../operators/numeric'
## types
* literals
  * -- represent a -- static value
* operations
* others
  * Check '../variables' & '/..specialFunction' & '../styleRules/ParentSelector'

## How to process SAAS files?
* `sass expressions.scss expressionsSCSS.css`
* `sass expressions.sass expressionsSASS.css`
